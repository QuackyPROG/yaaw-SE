{
  "schema": "yaaw.generic-command-runtime/v1",
  "transport": "STDIO_JSON",
  "request_schema": "yaaw.agent-eval-request/v1",
  "response_schema": "yaaw.agent-eval-result/v1",
  "identity_required": ["runtime_id", "provider", "model", "external=true"],
  "gateway_boundary": "EXTERNAL_WRAPPER_MUST_ENFORCE_YAAW_GATEWAY",
  "trace_contract": {
    "required_events": ["GATEWAY_ALLOWED", "ACTION_RESULT"],
    "correlation_fields": ["run_id", "trace_id", "span_id"]
  }
}
