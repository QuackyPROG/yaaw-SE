{
  "schema": "yaaw.agent-eval/v1",
  "id": "CI-AGENT-LOOP-FIXTURE",
  "task": "Exercise the complete repeated-trial, outcome-grading, trace-grading and stochastic-reporting path without invoking a real model or network provider.",
  "attempts": 4,
  "k": [1, 2, 3],
  "outcome_grader": {
    "expected_exit_code": 0,
    "output_contains": ["fixture success"],
    "output_excludes": ["forbidden-output"]
  },
  "trace_grader": {
    "require_events": ["GATEWAY_ALLOWED"],
    "forbid_events": ["POLICY_VIOLATION", "SCOPE_DRIFT", "QA_ESCAPE"],
    "require_correlation": true
  },
  "thresholds": {
    "min_pass_rate": 0.75,
    "min_trace_pass_rate": 1.0,
    "max_policy_violations": 0
  },
  "fixture": {
    "sequence": [true, true, false, true]
  }
}
