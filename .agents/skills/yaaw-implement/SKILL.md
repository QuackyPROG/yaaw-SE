---
name: yaaw-implement
description: Implement exactly one controller-admitted bounded contract using only relevant yaaw-core expertise and repository context.
---

# yaaw-implement

Load `_yaaw-core/workflows/implement/workflow.md` and only modules applicable to the admitted ticket. Build the bounded change, verify it, and return evidence. Ordinary review defects repair the same contract; material contract/architecture invalidation returns REPLAN rather than silent scope expansion.

## Artifact contract

Canonical machine contract: `.agents/artifacts.json` -> `contracts.skills.yaaw-implement`.

Resolve `.agents/artifacts.json` and `.agents/authority.json`. The admitted allowed-write contract is the mutation ceiling.
