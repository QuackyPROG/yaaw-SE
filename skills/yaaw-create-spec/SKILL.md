# yaaw-create-spec

ROLE: `planner`
WORKFLOW: `planning.create-spec`

Direct shortcut to the canonical create-spec workflow. It does not bypass readiness preconditions and contains no separate spec-generation intelligence.
