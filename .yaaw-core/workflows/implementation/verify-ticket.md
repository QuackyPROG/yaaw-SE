# Verify ticket

Run the tests/checks required by the ticket plus targeted regression checks justified by the changed surface. Record commands, results, and repository state in `.yaaw/evidence/` when practical.

Verification proves implementation evidence, not acceptance. Only Reviewer may PASS the ticket.
