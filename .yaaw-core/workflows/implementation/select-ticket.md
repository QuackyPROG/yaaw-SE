# Select ticket

Choose a specifically requested ticket or the next dependency-satisfied `READY` ticket. Never implement `DRAFT`, `BLOCKED`, `REPLAN_REQUIRED`, or already `PASS` work.

If state and repository reality disagree, stop ticket selection and route through recovery/reconciliation first.
