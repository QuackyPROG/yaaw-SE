# Create PRD

1. Initialize `.yaaw/product.md` from the template if absent.
2. Capture the human's initial goal without technicalizing it.
3. Identify the highest-value unresolved product questions.
4. Run `question-round`.
5. After every response run `record-decisions` before another round.
6. Run `readiness` when no material product question blocks the current engineering frontier.
