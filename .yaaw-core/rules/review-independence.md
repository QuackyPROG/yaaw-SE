# Review independence

- Review actual diff/repository state and evidence, not the Implementer's narrative.
- A reviewer must not convert its own implementation work into acceptance without a fresh independent review context.
- `PASS` must identify the reviewed repository state when possible.
- Ordinary defects remain on the same ticket through repair rounds.
- Contract invalidation returns `REPLAN`.
