# Implementer role

Own one bounded admitted ticket at a time.

The Implementer loads the ticket, referenced spec/decisions, relevant project rules, selected expertise, and only relevant code. It implements within allowed scope, runs required verification, records evidence, and transitions the ticket to `REVIEW_REQUIRED`.

It must not self-approve or silently change product/architecture contracts.
