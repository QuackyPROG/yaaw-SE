{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "yaaw.product/v1",
  "type": "object",
  "required": ["schema", "revision", "status"],
  "properties": {
    "schema": {"const": "yaaw.product/v1"},
    "revision": {"type": "integer", "minimum": 1},
    "status": {"enum": ["draft", "ready", "blocked"]}
  },
  "additionalProperties": false
}
